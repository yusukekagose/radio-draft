<%= form_for @radio, html: {class: 'radio-form', id: 'radio-form'} do |f| %>
  <div class="radio-form__field">
    <%= f.text_field :name, placeholder: "名前",class: "radio-form__input" %>
  </div>
  <div class="radio-form__field">
    <%= f.text_field :url, placeholder: "url",class: "radio-form__input" %>
  </div>
  <!-- <div class="radio-form__field">
    <%= f.text_field :img, placeholder: "img",class: "radio-form__input" %>
  </div> -->
  <div class="radio-form__field">
    <%= f.collection_select :station_id, Station.all, :id, :name, {}, { autofocus: 'false', class: 'radio-form__select'} %>
  </div>

  <!-- <div id="speaker_fields" class="radio-form__field">
    <% @radio.speakers.each do |speaker| %>
    <%= f.fields_for :speakers, speaker do |speakers_fields| %>
      <%= speakers_fields.text_field :name, placeholder: "出演者",class: "radio-form__input" %>
    <% end %>
    <% end %>
  </div> -->

  <div class="radio-form__field">
    <%= f.submit "送信", class: "radio-form__btn btn" %>
  </div>
<% end %>


<% Speaker.all.each do |speaker| %>
<div>
  <%= speaker.name %>
</div>
  
<% end %>

